# Scope

This repo is a corpus-first export of five QC-complete recipe slices. It does not include the parallel viewer, the alignment pipeline, or generated cache noise.

The Aëtius Book 1 pre-emended backup and comparison viewers are retained only as provenance. They are not peer recipe collections in the normalized data contract.
